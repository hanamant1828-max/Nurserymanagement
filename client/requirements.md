## Packages
recharts | Dashboard analytics and charts
date-fns | Date formatting for display
framer-motion | Smooth animations for page transitions and interactions

## Notes
Authentication uses session cookies (credentials: "include")
API endpoints return 401 for unauthorized access, redirect to login
Date fields are stored as text strings (YYYY-MM-DD) in backend
